repositories:
  - name: stable
    url: https://charts.helm.sh/stable

releases:
  - name: nginx
    namespace: default
    chart: stable/nginx-ingress
    version: 1.41.3
    values:
      - platform.yaml
