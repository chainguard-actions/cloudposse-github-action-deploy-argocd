name: Test kube-version input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Test with explicit kube-version (expects failure at checkout step)
        id: action-kube-version
        uses: .
        continue-on-error: true
        with:
          cluster: "https://github.com/nonexistent-org-xyz-123/nonexistent-argocd-config-repo.git"
          path: "tests/fixtures/helmfile/helmfile.yaml"
          toolchain: helmfile
          operation: deploy
          environment: preview
          repository: "test-owner/test-app"
          ref: "abc1234def5678"
          namespace: "test-namespace"
          application: "test-app"
          image: "nginx"
          image-tag: "1.21.0"
          kube-version: "1.28"
          github-pat: ${{ secrets.GITHUB_TOKEN }}

      - name: Assert action failed (kube-version input accepted)
        env:
          OUTCOME: ${{ steps.action-kube-version.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should fail because the ArgoCD config repo doesn't exist
          # This confirms kube-version input is accepted and early steps work
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail (nonexistent repo), got: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly processed kube-version input and failed at checkout"
